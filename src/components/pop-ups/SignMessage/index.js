import React from "react";

import StyledMessage from "./StyledMessage";

const SignMessage = () => {
  return (
    <StyledMessage>Pro přidání do oblíbených se musíš přihlásit.</StyledMessage>
  );
};

export default SignMessage;
